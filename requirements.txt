# Replication environment for "Global Inflation Forecasting".
# Requires Python 3.9 (TensorFlow 2.7.0 has no wheels for Python 3.10+).
#
# Install with uv:
#   uv venv --python 3.9 .venv
#   uv pip install -r requirements.txt --link-mode=copy
#   (--link-mode=copy is required when the project lives in OneDrive/Dropbox)
# or with pip:
#   py -3.9 -m venv .venv
#   .venv\Scripts\activate            # Windows  (source .venv/bin/activate on Unix)
#   pip install -r requirements.txt

# core scientific stack
numpy==1.22.4
scipy==1.13.1
scikit-learn==1.0.1
pandas==1.3.5
joblib==1.0.1
threadpoolctl==2.1.0
openpyxl==3.1.5

# benchmark / factor models
statsmodels==0.14.1
patsy==1.0.2

# tree / boosting models
xgboost==2.1.4
optuna==3.6.1

# autoencoder factors + neural-net forecasts
tensorflow==2.7.0
keras==2.7.0
protobuf==3.19.6
tensorboard==2.6.0
h5py==3.14.0
