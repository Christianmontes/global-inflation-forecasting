# Environment for the FIGURE scripts (Figures/*.py) only.
# Kept separate from the forecasting env (requirements.txt) because shap pulls a
# newer joblib that is unreliable to install into the OneDrive-synced .venv_repl39
# (OneDrive locks files mid-install). This mirrors the paper's own split:
# forecasts were produced on grendel, figures locally on a different system.
#
# Build this env OUTSIDE the OneDrive folder, e.g.:
#   uv venv --python 3.9 %LOCALAPPDATA%\figenv
#   uv pip install --python %LOCALAPPDATA%\figenv\Scripts\python.exe -r requirements-figures.txt
# then run, e.g.:
#   %LOCALAPPDATA%\figenv\Scripts\python.exe "Figures\cssed_plots.py"

# same core pins as requirements.txt (so Functions imports identically for the
# two figure scripts that import it)
numpy==1.22.4
scipy==1.13.1
scikit-learn==1.0.1
pandas==1.3.5
joblib==1.0.1
threadpoolctl==2.1.0
openpyxl==3.1.5
statsmodels==0.14.1
patsy==1.0.2
xgboost==2.1.4
optuna==3.6.1
tensorflow==2.7.0
keras==2.7.0
protobuf==3.19.6
tensorboard==2.6.0
h5py==3.14.0

# figure-only dependencies
matplotlib==3.6.3
shap
geopandas
mapclassify
